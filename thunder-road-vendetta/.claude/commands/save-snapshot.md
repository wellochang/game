更新目前專案的 CLAUDE.md 狀態快照。

步驟：
1. 在目前工作目錄找 CLAUDE.md；找不到則往上一層找，最多找到 claudeAgent 根目錄為止
2. 將「上次工作」更新為今天日期（YYYY-MM-DD）
3. 將「下次優先項」更新為：$ARGUMENTS（若無提供，從目前對話內容推斷最合理的下一步）
4. 直接編輯檔案，不需詢問確認

完成後輸出一行：已更新 `<路徑>/CLAUDE.md`
